// This file is our database of rules for Homotopy Groups of Spheres.
// The rules are checked in order, so place more specific ones first.

const piStarRules = [
  // --- Trivial Case ---
  {
    condition: (n, k) => k < n && k >= 0 && n > 0,
    result: "The group is trivial, i.e., equal to \\(0\\).",
    reference: "A fundamental theorem of homotopy groups states that spheres are (n-1)-connected."
  },

  // --- Infinite Cases ---
  {
    condition: (n, k) => k === n && n >= 1,
    result: "The group \\(\\pi_n(S^n)\\) is isomorphic to the integers, \\(\\mathbb{Z}\\).",
    reference: "This is a consequence of the Hurewicz Theorem, which relates the first non-trivial homotopy and homology groups of a space."
  },
  {
    condition: (n, k) => n === 2 && k === 3,
    result: "The group \\(\\pi_3(S^2)\\) is isomorphic to the integers, \\(\\mathbb{Z}\\), and is generated by the Hopf map.",
    reference: "This was a surprising discovery related to the Hopf Fibration."
  },
  {
    condition: (n, k) => k === 2 * n - 1 && n > 1 && n % 2 === 0,
    result: "The group \\(\\pi_{2n-1}(S^n)\\) contains a direct summand isomorphic to the integers, \\(\\mathbb{Z}\\), and is therefore infinite.",
    reference: "This follows from Serre's work on rational homotopy groups, a major result derived from his spectral sequence and the generalized Hurewicz theorem."
  },

  // --- Finiteness Rule ---
  {
    condition: (n, k) => {
      const isSerreInfinite = (n > 1 && n % 2 === 0 && k === 2 * n - 1);
      const isHopfInfinite = (n === 2 && k === 3);
      return k > n && !isSerreInfinite && !isHopfInfinite;
    },
    result: "This group is finite.",
    reference: "A highly non-trivial result established by Jean-Pierre Serre, showing that for \\(k>n\\), the homotopy groups \\(\\pi_k(S^n)\\) are all finite abelian groups, except for the specific infinite families."
  },

  // --- Additional Properties (Freudenthal) ---
  {
    // THIS IS THE CORRECTED RULE
    condition: (n, k) => k < 2 * n - 1,
    result: "This group is in the stable range, defined by the inequality \\(k < 2n-1\\). In this range, the Freudenthal Suspension Theorem applies, guaranteeing that the suspension homomorphism is an isomorphism: \\(\\pi_{k}(S^n) \\cong \\(\\pi_{k+1}(S^{n+1})\\).",
    reference: "This theorem is a cornerstone for calculating stable homotopy groups. Note that the group may also satisfy other properties, such as being trivial or infinite."
  }
];